# =============================================================================
# FORECASTING AGENT - ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and update the values
# IMPORTANT: Change SECRET_KEY and API_SECRET_KEY before production use!
# =============================================================================

# -----------------------------------------------------------------------------
# SECURITY (REQUIRED - CHANGE THESE!)
# -----------------------------------------------------------------------------
# Secret key for API authentication (generate a strong random string)
API_SECRET_KEY=change-this-to-a-strong-random-secret-key-min-32-chars

# Secret key for JWT token signing (generate a different strong random string)
SECRET_KEY=change-this-to-another-strong-random-secret-key-min-32-chars

# JWT algorithm (HS256 is recommended)
ALGORITHM=HS256

# Token expiration time in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Allowed IP addresses (comma-separated, supports CIDR notation)
# For local testing: 127.0.0.1,::1,localhost
# For production: Add your Capexplan server IPs
ALLOWED_IPS=127.0.0.1,::1,localhost

# -----------------------------------------------------------------------------
# OLLAMA LLM CONFIGURATION
# -----------------------------------------------------------------------------
# Ollama server URL
OLLAMA_HOST=http://localhost:11434

# Model to use (must be pulled with: ollama pull qwen2.5:7b)
OLLAMA_MODEL=qwen2.5:7b

# LLM temperature (0.0-1.0, higher = more creative)
LLM_TEMPERATURE=0.7

# LLM request timeout in seconds
LLM_TIMEOUT=60

# -----------------------------------------------------------------------------
# API CONFIGURATION
# -----------------------------------------------------------------------------
# API title (shown in docs)
API_TITLE=Forecasting Agent API

# API version
API_VERSION=1.0.0

# API prefix
API_V1_PREFIX=/api/v1

# Debug mode (set to False in production)
DEBUG=False

# Rate limit per minute
RATE_LIMIT_PER_MINUTE=60

# -----------------------------------------------------------------------------
# CORS CONFIGURATION
# -----------------------------------------------------------------------------
# Allowed origins for CORS (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# -----------------------------------------------------------------------------
# LOGGING
# -----------------------------------------------------------------------------
# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Log file path
LOG_FILE=logs/forecasting-agent.log

# -----------------------------------------------------------------------------
# CAPEXPLAN INTEGRATION (Optional - for production)
# -----------------------------------------------------------------------------
# CAPEXPLAN_API_URL=https://api.capexplan.com/v1
# CAPEXPLAN_API_KEY=your-capexplan-api-key

# =============================================================================
# QUICK START:
# 1. Copy this file: cp .env.example .env
# 2. Change API_SECRET_KEY to a random 32+ character string
# 3. Change SECRET_KEY to a different random 32+ character string
# 4. Run: uvicorn app.main:app --reload
# =============================================================================
